{
  "name": "parkerhamilton-ca",
  "version": "0.1.0-foundation",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "npm run check:astro && npm run check:ts",
    "check:astro": "astro check",
    "check:ts": "tsc --noEmit --project tsconfig.json",
    "test": "node --test tests/*.test.mjs && node --experimental-strip-types --test tests/coast-fi-invariants.test.ts && node --experimental-strip-types tests/coast-fi-v2-runner.ts tests/fixtures/coast-fi/coast-fi-test-cases-v2.json",
    "test:browser": "playwright test",
    "qa:contrast": "python tests/contrast_check.py"
  },
  "dependencies": {
    "astro": "7.2.8"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "@playwright/test": "1.63.0",
    "@types/node": "24.10.1",
    "typescript": "5.9.3"
  }
}
